spring:
  application:
    name: xiangyun-prescription

  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
        import-check:
          enabled: false
  autoconfigure:
    exclude:
      - com.alibaba.cloud.nacos.NacosConfigAutoConfiguration
      - com.alibaba.cloud.nacos.discovery.NacosDiscoveryClientAutoConfiguration

  datasource:
    url: jdbc:postgresql://localhost:5432/xiangyun_zhifang?currentSchema=public
    username: xiangyun_app
    password: xiangyun_dev_2026
    driver-class-name: org.postgresql.Driver
    type: com.alibaba.druid.pool.DruidDataSource

  data:
    redis:
      host: localhost
      port: 6379
      password: xiangyun_redis_2026

server:
  port: 8087

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

logging:
  level:
    com.xiangyun.prescription: DEBUG
